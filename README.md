# Hosebot

**Experiments**
To design an experiment, import `data_acquisition` and `motor_control`, see `ramp_up_down_exp3.py` as sample controller. Always run `AutoGROQS6.autocalibration()` before using motor.